-- Run as superuser, e.g.:
--   psql -U postgres -h localhost -d postgres -f scripts/create-database.sql
--
-- If it already exists: ERROR already exists — ignore, or use create-database.ps1 (idempotent).

CREATE DATABASE fleet_management;
